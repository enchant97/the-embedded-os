[package]
name = "libsys"
version = "0.1.0"
edition = "2024"

[dependencies]
kernel_abi = { path = "../kernel_abi" }
nostd = { version = "0.1", default-features = false, features = [ "io" ] }
embassy-sync = "0.8"
