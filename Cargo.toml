[workspace]
resolver = "3"
members = ["kernel","kernel_abi", "libsys", "loader", "shell"]

[profile.release]
opt-level = "z"
strip = true # this will cause defmt to not work
lto = true
panic = "abort"
codegen-units = 1
