[workspace]
resolver = "3"
members = ["kernel","kernel_abi", "libsys", "loader", "shell", "st7920"]

[profile.release]
opt-level = "z"
strip = true # this will cause defmt to not work
lto = true
panic = "abort"
codegen-units = 1
